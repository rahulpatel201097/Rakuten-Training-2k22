server:
  port: 8082
  
spring:
  application:
    name: PENSIONER-DETAIL-MODULE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8084/eureka/

  instance:
    hostname: localhost