server:
  port: 8081

spring:
  application:
    name: PROCESS-PENSION-MODULE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8084/eureka/

  instance:
    hostname: localhost